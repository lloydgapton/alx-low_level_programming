#include "main.h"
/**
 * _puts_recursion -write a function that prints a string, followed by a nea line
 * @s: this is the input string.
 */
 void _puts_recursion(char *s)
{
	if (*s == '\0')
	{
		_putchar('\n');
		return;
	}
	_putchar(*s);
	s++;
	_puts_recursion(s);
}#include "main.h"
/**
 * _puts_recursion -write a function that prints a string, followed by a nea line
 * @s: this is the input string.
 */
 void _puts_recursion(char *s)
{
	if (*s == '\0')
	{
		_putchar('\n');
		return;
	}
	_putchar(*s);
	s++;
	_puts_recursion(s);
}#include "main.h"
/**
 * _puts_recursion -write a function that prints a string, followed by a nea line
 * @s: this is the input string.
 */
 void _puts_recursion(char *s)
{
	if (*s == '\0')
	{
		_putchar('\n');
		return;
	}
	_putchar(*s);
	s++;
	_puts_recursion(s);
}
