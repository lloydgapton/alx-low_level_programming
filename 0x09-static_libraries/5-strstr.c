#include "main.h"
#include <stdio.h>

/**
 * _strstr - locates a sybstring inside a string
 * @haystack :string to search.
 * @needle: substring to search for
 * Return returns a pointer to location of substring inside haystack or NULL
 */
char *_strstr(char *haystack, char *needle)
{
}
